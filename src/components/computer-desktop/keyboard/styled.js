import styled from 'styled-components'

export const Svg = styled.svg`
  path {
    fill: black;
  }

  .key {
    fill: white;
  }

  .board {
    fill: black;
  }
`

export const HiddenText = styled.span`
  visibility: hidden;
  transform: scale(0);
`
