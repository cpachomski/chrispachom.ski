import styled from 'styled-components'

export const Svg = styled.svg`
  path {
    fill: black;
    stroke: black;
  }
`

export const HiddenText = styled.span`
  visibility: hidden;
  transform: scale(0);
`
