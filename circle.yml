dependencies:
    override:
        - sudo pip install awscli

test:
  override:
    - echo "Tests are currently disabled. To disable, remove this block from circle.yml"

deployment:
    prod:
      branch: master
      commands:
        - aws s3 sync /home/ubuntu/index.html s3://s3.amazonaws.com/chrispachom.ski --delete
