dependencies:
    override:
        - sudo pip install awscli

test:
  override:
    - echo "Tests are currently disabled. To disable, remove this block from circle.yml"

deployment:
    prod:
      branch: master
      commands:
        - aws s3 sync . s3://chrispachom.ski --delete
        - aws cloudfront create-invalidation --distribution-id $distributionId --paths '/*''
